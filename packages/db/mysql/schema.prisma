generator client {
  provider = "prisma-client-js"
  output   = "../prisma-clients/mysql"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Post {
  id    String @id @default(cuid())
  title String
}
